<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.logisticappproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SiteNotificationDetailByRoomActivity"></activity>
        <activity android:name=".RoomResumeActivity" />
        <activity android:name=".ScanMaterialActivity" />
        <activity android:name=".DepartureRegisterActivity" />
        <activity android:name=".MaterialSearchActivity" />
        <activity android:name="com.example.activities.delegate.ScanMaterialActivity" />
        <activity android:name="com.example.activities.delegate.MaterialDetailActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>